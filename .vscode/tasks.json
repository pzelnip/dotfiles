{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Symbolic link current file to ~/bin",
            "type": "shell",
            "command": "./ln_file_in_home_bin.sh ${fileBasename}"
        }
    ]
}
